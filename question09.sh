# Create and switch to branch1
git checkout -b branch1

# Create a new file file3
touch file3

# Stage the file3
git add file3

# Commit the staged changes
git commit -m "Add file3 to branch1"
